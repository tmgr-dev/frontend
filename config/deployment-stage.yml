apiVersion: apps/v1
kind: Deployment
metadata:
  name: tmgr-ui-stage
  labels:
    app: tmgr-ui-stage
  namespace: applications
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tmgr-ui-stage
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: tmgr-ui-stage
    spec:
      containers:
        - name: tmgr-ui-stage
          image: <IMAGE>
          ports:
            - containerPort: 80
              name: http
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
      imagePullSecrets:
        - name: dockerhub-secret
---
apiVersion: v1
kind: Service
metadata:
  name: tmgr-ui-stage-service
  namespace: applications
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: tmgr-ui-stage
---
apiVersion: v1
kind: Service
metadata:
  name: tmgr-ui-stage-service-proxy
  namespace: default
spec:
  type: ExternalName
  externalName: tmgr-ui-stage-service.applications.svc.cluster.local
